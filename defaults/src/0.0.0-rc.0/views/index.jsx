require('../assets/css/main.scss');

const React = require('react');

const Body = React.createClass({
    render() {
        return (
            <h1>Hello world!</h1>
        );
    }
});

module.exports = Body;